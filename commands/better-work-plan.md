---
description: "Better Work plan mode. Clarifies the task, defines success criteria, and creates a lightweight execution plan."
argument-hint: "[task description]"
---

Load the `better-work` skill and apply `plan` mode to the current task.

Prioritize:

- clarifying the goal and constraints
- deciding whether workflow files are warranted
- breaking the task into verifiable slices
- recording the highest-leverage next step
